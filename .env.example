# Where the admin API lives.
#
# Read at BUILD time, not at runtime — Vite bakes it into the bundle. Changing
# it on the host and restarting does nothing; the site has to be rebuilt.
#
# Leave it unset for local work and it falls back to http://localhost:4000.
VITE_API_URL=https://sgd-api.onrender.com
